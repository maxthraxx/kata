---
phase: 00-hard-fork-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .git/config
  - package.json
autonomous: true

must_haves:
  truths:
    - "Git has no upstream remote"
    - "Origin points to gannonh/kata"
    - "package.json has gannonh as author"
    - "package.json version is 0.1.0"
    - "No glittercowboy references in package.json"
  artifacts:
    - path: "package.json"
      provides: "Package identity"
      contains: "gannonh/kata"
  key_links:
    - from: "package.json repository.url"
      to: "git remote origin"
      via: "matching repo URL"
      pattern: "gannonh/kata"
---

<objective>
Establish new project identity by updating git configuration and package.json.

Purpose: Sever ties from upstream glittercowboy/get-shit-done and establish gannonh/kata as the authoritative source.
Output: Git configured with gannonh/kata as origin (no upstream), package.json updated with new identity.
</objective>

<execution_context>
@~/.claude/kata/workflows/execute-plan.md
@~/.claude/kata/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-hard-fork-rebrand/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure git remotes</name>
  <files>.git/config</files>
  <action>
    1. Remove upstream remote if it exists:
       `git remote remove upstream` (ignore error if not exists)

    2. Update origin to new repository:
       `git remote set-url origin git@github.com:gannonh/kata.git`

    3. Verify configuration:
       `git remote -v` should show only origin pointing to gannonh/kata
  </action>
  <verify>
    Run `git remote -v` and confirm:
    - No "upstream" remote listed
    - origin shows `gannonh/kata.git`
  </verify>
  <done>Git remotes configured with only origin (gannonh/kata), upstream removed</done>
</task>

<task type="auto">
  <name>Task 2: Update package.json identity</name>
  <files>package.json</files>
  <action>
    Update package.json with the following changes:

    1. Keep "name": "kata-cli" (already correct)
    2. Change "version": "1.6.4" to "version": "0.1.0"
    3. Remove "by TACHES" from description, keep rest:
       "description": "A meta-prompting, context engineering and spec-driven development system for Claude Code."
    4. Change "author": "TACHES" to "author": "gannonh"
    5. Update repository.url to: "git+https://github.com/gannonh/kata.git"

    Final package.json should have no references to:
    - glittercowboy
    - TACHES
    - get-shit-done
  </action>
  <verify>
    Run: `grep -E "(glittercowboy|TACHES|get-shit-done)" package.json`
    Should return no matches (exit code 1).

    Run: `node -e "console.log(require('./package.json').version)"`
    Should output: 0.1.0
  </verify>
  <done>package.json has gannonh as author, version 0.1.0, repository gannonh/kata, no old references</done>
</task>

</tasks>

<verification>
After both tasks:
1. `git remote -v` shows only origin pointing to gannonh/kata
2. `grep -E "(glittercowboy|TACHES|get-shit-done)" package.json` returns no matches
3. `node -e "console.log(require('./package.json').author)"` outputs "gannonh"
</verification>

<success_criteria>
- Git upstream remote removed
- Git origin points to gannonh/kata
- package.json author is gannonh
- package.json version is 0.1.0
- package.json has no glittercowboy, TACHES, or get-shit-done references
</success_criteria>

<output>
After completion, create `.planning/phases/00-hard-fork-rebrand/00-01-SUMMARY.md`
</output>
