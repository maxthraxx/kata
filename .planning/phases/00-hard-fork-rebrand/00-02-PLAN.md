---
phase: 00-hard-fork-rebrand
plan: 02
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - CLAUDE.md
  - README.md
  - bin/install.js
autonomous: true

must_haves:
  truths:
    - "CLAUDE.md describes standalone project workflow"
    - "README.md has no upstream fork references"
    - "Install script has no TACHES references"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project development instructions"
      min_lines: 10
    - path: "README.md"
      provides: "Project documentation"
      contains: "kata-cli"
    - path: "bin/install.js"
      provides: "CLI installer"
      contains: "gannonh"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "consistent package name"
      pattern: "kata-cli"
---

<objective>
Update core documentation and install script to reflect standalone Kata project.

Purpose: Remove all fork-related documentation and upstream references, establish Kata as an independent project by gannonh.
Output: CLAUDE.md with standalone workflow, README.md cleaned of old references, install.js banner updated.
</objective>

<execution_context>
@~/.claude/kata/workflows/execute-plan.md
@~/.claude/kata/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-hard-fork-rebrand/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md for standalone project</name>
  <files>CLAUDE.md</files>
  <action>
    Replace entire CLAUDE.md with standalone project instructions.

    Remove:
    - "This is a fork of [glittercowboy/get-shit-done]" line
    - Upstream remote references
    - Instructions about syncing from upstream
    - PR conventions mentioning upstream

    New content should be:
    ```markdown
    # Kata Development

    ## Git Workflow

    **For all changes:**
    1. Create a feature branch from main: `git checkout -b feature/description`
    2. Make commits on the feature branch
    3. Push to origin: `git push origin feature/description`
    4. Open PR for review

    **Never commit directly to main.** All work happens on feature branches.
    ```

    Keep it minimal - this is a personal project, not a complex multi-contributor repo.
  </action>
  <verify>
    Run: `grep -E "(glittercowboy|upstream|fork)" CLAUDE.md`
    Should return no matches.

    File should exist and have basic git workflow instructions.
  </verify>
  <done>CLAUDE.md describes standalone workflow with no fork/upstream references</done>
</task>

<task type="auto">
  <name>Task 2: Clean README.md references</name>
  <files>README.md</files>
  <action>
    Update README.md to remove old project references:

    1. Replace any glittercowboy/get-shit-done URLs with gannonh/kata
    2. Replace TACHES mentions with gannonh or just remove
    3. Keep "kata-cli" as the package name (already correct)
    4. Update any badge URLs from glittercowboy to gannonh
    5. Remove or update "Credits" section if it references original author

    Search patterns to find and replace:
    - `glittercowboy/get-shit-done` -> `gannonh/kata`
    - `TACHES` or `TÂCHES` -> `gannonh` or remove
    - Any GitHub links to old repo

    Do NOT change:
    - `kata-cli` (keep as package name)
    - `/kata:` command prefix
    - Core feature descriptions
  </action>
  <verify>
    Run: `grep -iE "(glittercowboy|TACHES|TÂCHES)" README.md`
    Should return no matches.

    Run: `grep "gannonh/kata" README.md`
    Should return at least one match (repo link).
  </verify>
  <done>README.md has no old project references, uses gannonh/kata URLs</done>
</task>

<task type="auto">
  <name>Task 3: Update install.js banner</name>
  <files>bin/install.js</files>
  <action>
    Update the banner in bin/install.js:

    1. Find line ~28 that says "by TÂCHES"
    2. Change to "by gannonh" or remove the attribution line entirely

    Current:
    ```javascript
    development system for Claude Code by TÂCHES.
    ```

    Change to:
    ```javascript
    development system for Claude Code.
    ```

    Do NOT change:
    - The KATA ASCII art banner
    - Installation logic
    - kata/kata-cli references (these are correct)
  </action>
  <verify>
    Run: `grep -i "TACHES" bin/install.js`
    Should return no matches.

    Run: `node bin/install.js --help`
    Should show help without TACHES mention.
  </verify>
  <done>Install script banner has no TACHES reference</done>
</task>

</tasks>

<verification>
After all tasks:
1. `grep -rE "(glittercowboy|TACHES|TÂCHES)" CLAUDE.md README.md bin/install.js` returns no matches
2. `grep "gannonh" README.md` returns matches
3. CLAUDE.md is simple standalone workflow (no upstream references)
</verification>

<success_criteria>
- CLAUDE.md has no fork/upstream references
- README.md uses gannonh/kata URLs
- No TACHES/TÂCHES mentions in core files
- Install script banner is clean
</success_criteria>

<output>
After completion, create `.planning/phases/00-hard-fork-rebrand/00-02-SUMMARY.md`
</output>
