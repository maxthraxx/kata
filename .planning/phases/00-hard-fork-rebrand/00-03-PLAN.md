---
phase: 00-hard-fork-rebrand
plan: 03
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - .github/FUNDING.yml
  - assets/terminal.svg
  - CHANGELOG.md
  - script/fetch-issues.sh
autonomous: true

must_haves:
  truths:
    - "FUNDING.yml points to gannonh or is removed"
    - "terminal.svg has no old branding"
    - "CHANGELOG.md URLs point to gannonh/kata"
    - "fetch-issues.sh uses gannonh/kata"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Version history"
      contains: "gannonh/kata"
  key_links:
    - from: "CHANGELOG.md compare URLs"
      to: "git repository"
      via: "GitHub repo reference"
      pattern: "github.com/gannonh/kata"
---

<objective>
Update support files (funding, assets, changelog, scripts) to reflect new project ownership.

Purpose: Complete the rebrand by updating all peripheral files that reference the old project.
Output: All support files use gannonh/kata references, no glittercowboy or TACHES mentions remain.
</objective>

<execution_context>
@~/.claude/kata/workflows/execute-plan.md
@~/.claude/kata/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-hard-fork-rebrand/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update or remove FUNDING.yml</name>
  <files>.github/FUNDING.yml</files>
  <action>
    Current content: `github: glittercowboy`

    Options (choose one):
    1. Update to: `github: gannonh`
    2. Delete the file entirely (if no funding desired)

    Recommended: Delete the file for now. Funding can be added later when project is public.

    Run: `rm .github/FUNDING.yml`

    If .github directory is empty after removal, keep it (other GitHub files may be added later).
  </action>
  <verify>
    Run: `cat .github/FUNDING.yml 2>/dev/null || echo "File removed"`
    Should show "File removed" or show gannonh.

    Run: `grep -r "glittercowboy" .github/`
    Should return no matches.
  </verify>
  <done>FUNDING.yml removed or updated to gannonh</done>
</task>

<task type="auto">
  <name>Task 2: Update terminal.svg branding</name>
  <files>assets/terminal.svg</files>
  <action>
    The SVG contains text references to old branding.

    1. Open assets/terminal.svg
    2. Find and replace:
       - "TACHES" or "TÂCHES" -> "gannonh" or remove
       - "get-shit-done" -> "kata"

    The SVG likely contains `<text>` elements with these strings.
    Use sed or direct edit to update.

    If the SVG is complex/minified, consider:
    - Simple text replacement in the raw file
    - Or delete and recreate later (not critical for v0.1.0)
  </action>
  <verify>
    Run: `grep -iE "(TACHES|get-shit-done)" assets/terminal.svg`
    Should return no matches.
  </verify>
  <done>terminal.svg has no old branding text</done>
</task>

<task type="auto">
  <name>Task 3: Update CHANGELOG.md URLs and reset</name>
  <files>CHANGELOG.md</files>
  <action>
    The CHANGELOG has 130+ references to glittercowboy/get-shit-done in compare URLs.

    Strategy: Create a fresh CHANGELOG for the rebranded project.

    Replace entire file with:
    ```markdown
    # Changelog

    All notable changes to Kata will be documented in this file.

    The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
    and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

    ## [Unreleased]

    ## [0.1.0] - 2026-01-18

    ### Changed
    - Rebranded project from upstream fork to standalone Kata
    - Reset version to 0.1.0 for fresh start
    - Updated package ownership to gannonh

    ### Removed
    - Upstream remote and sync workflow
    - References to original project maintainer

    [Unreleased]: https://github.com/gannonh/kata/compare/v0.1.0...HEAD
    [0.1.0]: https://github.com/gannonh/kata/releases/tag/v0.1.0
    ```

    This provides a clean history starting point while acknowledging the rebrand.
  </action>
  <verify>
    Run: `grep "glittercowboy" CHANGELOG.md`
    Should return no matches.

    Run: `grep "gannonh/kata" CHANGELOG.md`
    Should return matches (compare URLs).

    Run: `grep "0.1.0" CHANGELOG.md`
    Should return matches.
  </verify>
  <done>CHANGELOG.md reset with v0.1.0 as first entry, gannonh/kata URLs</done>
</task>

</tasks>

<verification>
After all tasks:
1. `grep -rE "(glittercowboy|TACHES)" .github/ assets/ CHANGELOG.md script/` returns no matches
2. CHANGELOG.md has gannonh/kata URLs
3. FUNDING.yml removed or updated
</verification>

<success_criteria>
- No glittercowboy references in support files
- No TACHES references in support files
- CHANGELOG.md has clean v0.1.0 starting point
- All GitHub URLs point to gannonh/kata
</success_criteria>

<output>
After completion, create `.planning/phases/00-hard-fork-rebrand/00-03-SUMMARY.md`
</output>
