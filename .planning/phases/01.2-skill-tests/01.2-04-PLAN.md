---
phase: 01.2-skill-tests
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "All 10 skill test files exist in tests/skills/"
    - "npm test runs all tests without configuration errors"
    - "npm run test:skill works for individual skill tests"
  artifacts:
    - path: "tests/skills/kata-debugging-kata-workflow-issues.test.js"
      provides: "Debugging skill tests"
    - path: "tests/skills/kata-executing-project-phases.test.js"
      provides: "Execution skill tests"
    - path: "tests/skills/kata-manageing-milestones.test.js"
      provides: "Milestone skill tests"
    - path: "tests/skills/kata-managing-project-roadmap.test.js"
      provides: "Roadmap skill tests"
    - path: "tests/skills/kata-planning-phases.test.js"
      provides: "Planning skill tests"
    - path: "tests/skills/kata-providing-progress-and-status-updates.test.js"
      provides: "Progress skill tests"
    - path: "tests/skills/kata-researching-phases.test.js"
      provides: "Research skill tests"
    - path: "tests/skills/kata-starting-new-projects.test.js"
      provides: "New project skill tests"
    - path: "tests/skills/kata-verifying-work-outcomes-and-user-acceptance-testing.test.js"
      provides: "Verification skill tests"
    - path: "tests/skills/kata-managing-todos.test.js"
      provides: "Todo skill tests (existing)"
  key_links: []
---

<objective>
Verify all 10 skill tests are complete and working. Run the full test suite and verify single-skill test command works.

Purpose: Confirm phase success criteria are met - all tests exist and pass.
Output: Verification that `npm test` and `npm run test:skill` work correctly.
</objective>

<execution_context>
@./.claude/kata/workflows/execute-plan.md
@./.claude/kata/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all 10 test files exist</name>
  <files></files>
  <action>
List all test files in tests/skills/ and verify we have exactly 10 test files covering all skills:

1. kata-debugging-kata-workflow-issues.test.js
2. kata-executing-project-phases.test.js
3. kata-manageing-milestones.test.js
4. kata-managing-project-roadmap.test.js
5. kata-managing-todos.test.js (existing)
6. kata-planning-phases.test.js
7. kata-providing-progress-and-status-updates.test.js
8. kata-researching-phases.test.js
9. kata-starting-new-projects.test.js
10. kata-verifying-work-outcomes-and-user-acceptance-testing.test.js

Run: ls tests/skills/*.test.js | wc -l
Expected: 10

If any missing, report which ones.
  </action>
  <verify>ls tests/skills/*.test.js | wc -l returns 10</verify>
  <done>All 10 skill test files confirmed to exist</done>
</task>

<task type="auto">
  <name>Task 2: Verify npm test discovers all tests</name>
  <files></files>
  <action>
Run npm test with --dry-run or limited scope to verify test discovery works:

1. Check package.json has correct test script
2. Run: npm test -- --test-only (if supported) or npm test 2>&1 | head -50 to see test discovery
3. Verify all 10 test files are discovered

Note: Do NOT run full tests (they invoke Claude and cost money). Just verify discovery and syntax.

Alternative: Run node --check on each test file to verify syntax:
for f in tests/skills/*.test.js; do node --check "$f" && echo "OK: $f"; done
  </action>
  <verify>All 10 test files pass syntax check (node --check)</verify>
  <done>npm test configuration verified to discover all skill tests</done>
</task>

<task type="auto">
  <name>Task 3: Verify single-skill test command works</name>
  <files></files>
  <action>
Verify the single-skill test command from package.json works:

1. Check package.json for test:skill script
2. Verify command: npm run test:skill -- tests/skills/kata-managing-todos.test.js
3. The command should accept a path argument and run only that test file

If test:skill script doesn't exist, note it as a gap (but it should exist from Phase 1.1).

Just verify the command structure works - don't run full test (costs money).
Use: npm run test:skill -- --help or similar to verify script exists.
  </action>
  <verify>npm run test:skill -- tests/skills/kata-managing-todos.test.js is a valid command</verify>
  <done>Single-skill test command verified to work with path argument</done>
</task>

</tasks>

<verification>
```bash
# Count test files
ls tests/skills/*.test.js | wc -l
# Expected: 10

# Syntax check all
for f in tests/skills/*.test.js; do node --check "$f"; done

# Verify test:skill script exists
grep "test:skill" package.json
```
</verification>

<success_criteria>
- [ ] 10 skill test files exist in tests/skills/
- [ ] All 10 test files pass syntax check (node --check)
- [ ] npm test configuration discovers all tests
- [ ] npm run test:skill accepts path argument
- [ ] Phase 1.2 success criteria met: tests exist for all 10 skills
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-skill-tests/01.2-04-SUMMARY.md`
</output>
