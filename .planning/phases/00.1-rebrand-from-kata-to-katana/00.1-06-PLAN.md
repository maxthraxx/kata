---
phase: 00.1-rebrand-from-kata-to-katana
plan: 06
type: execute
wave: 5
depends_on: ["00.1-04", "00.1-05"]
files_modified: [.planning/**/*.md, CLAUDE.md, CHANGELOG.md, .gitignore, KATANA-STYLE.md]
autonomous: true

must_haves:
  truths:
    - "Active planning references use Katana naming"
    - "ROADMAP.md uses Katana terminology"
    - "CLAUDE.md uses Katana terminology"
    - "No /gsd: or /kata: references remain anywhere"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap with Katana naming"
      contains: "Katana"
    - path: "CLAUDE.md"
      provides: "Claude instructions with Katana naming"
      contains: "Katana"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "commands/katana/"
      via: "command references"
      pattern: "/katana:"
---

<objective>
Update planning files, CLAUDE.md, and miscellaneous files with Katana naming. Perform final verification.

Purpose: Complete the rebrand by updating all remaining files and verify no Kata references remain.
Output: Complete Katana rebrand with verification report.
</objective>

<execution_context>
@~/.claude/katana/workflows/execute-plan.md
@~/.claude/katana/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00.1-rebrand-from-kata-to-katana/00.1-RESEARCH.md
@.planning/phases/00.1-rebrand-from-kata-to-katana/00.1-04-SUMMARY.md
@.planning/phases/00.1-rebrand-from-kata-to-katana/00.1-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update active planning files</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <action>
    Update ACTIVE planning files (not historical phase documents):

    1. ROADMAP.md:
       - Replace "Kata" with "Katana" in title and overview
       - Replace /kata: with /katana: in command references
       - Replace /gsd: with /katana: in command references
       - Update phase titles if they reference "Kata"

    2. STATE.md:
       - Replace "Kata" with "Katana" in references
       - Update command references

    3. PROJECT.md:
       - Replace "Kata" with "Katana" in project name and descriptions
       - Update any command or path references

    4. REQUIREMENTS.md:
       - Update any Kata references to Katana

    IMPORTANT: Keep historical references in phase folders as-is (Phase 0 docs talk about "Kata" because that was the name at the time).
  </action>
  <verify>grep -c "Katana" .planning/ROADMAP.md (should show matches)</verify>
  <done>Active planning files use Katana naming</done>
</task>

<task type="auto">
  <name>Task 2: Update miscellaneous files</name>
  <files>CLAUDE.md, CHANGELOG.md, .gitignore, KATANA-STYLE.md</files>
  <action>
    Update remaining root and config files:

    1. CLAUDE.md:
       - Replace "Kata" with "Katana"
       - Update any command or path references

    2. CHANGELOG.md:
       - Update header/title to Katana
       - Keep historical entries as-is (they document what happened)

    3. .gitignore:
       - Update any kata references to katana

    4. KATANA-STYLE.md (renamed in Plan 02):
       - Update internal references from Kata to Katana

    Also check for any other files at root that need updating.
  </action>
  <verify>grep -l "kata" CLAUDE.md CHANGELOG.md .gitignore | wc -l (should be 0, except historical in CHANGELOG)</verify>
  <done>All miscellaneous files use Katana naming</done>
</task>

<task type="auto">
  <name>Task 3: Final verification and commit</name>
  <files>all</files>
  <action>
    1. Run comprehensive verification:
       ```bash
       # Check for remaining kata references (excluding .git)
       grep -ri "kata" --include="*.md" --include="*.js" --include="*.json" . | grep -v ".git" | grep -v "node_modules" | grep -v "katana"

       # Check for remaining gsd: prefixes
       grep -ri "/gsd:" --include="*.md" . | grep -v ".git"
       ```

    2. Fix any remaining references found

    3. Stage all changes:
       ```bash
       git add -A
       ```

    4. Create final commit:
       "refactor(00.1): complete Kata to Katana rebrand

       - Updated all directories (kata/ -> katana/, commands/kata/ -> commands/katana/)
       - Renamed all kata-* files to katana-*
       - Updated package.json identity (katana-cli)
       - Updated all command prefixes (/kata:, /gsd: -> /katana:)
       - Updated all @-references and file paths
       - Verified no orphaned kata references remain"

    5. Do NOT push - user will review and push
  </action>
  <verify>grep -ri "kata" . --include="*.md" --include="*.js" --include="*.json" | grep -v ".git" | grep -v "node_modules" | grep -v "katana" | wc -l (should be 0 or only historical)</verify>
  <done>Rebrand complete, all changes committed, ready for push</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -ri "/kata:" . | grep -v .git | grep -v katana` returns 0 matches
- [ ] `grep -ri "/gsd:" . | grep -v .git` returns 0 matches
- [ ] `grep -ri "kata/" . | grep -v .git | grep -v katana` returns 0 matches (or only historical)
- [ ] All katana directories exist and have correct content
- [ ] package.json shows "katana-cli" and "katana" binary
- [ ] git log shows all rebrand commits
</verification>

<success_criteria>
- All active files use Katana naming
- No orphaned /kata: or /gsd: command prefixes
- No orphaned kata/ path references
- All changes committed with descriptive messages
- Verification passes with no unexpected kata references
</success_criteria>

<output>
After completion, create `.planning/phases/00.1-rebrand-from-kata-to-katana/00.1-06-SUMMARY.md`
</output>
