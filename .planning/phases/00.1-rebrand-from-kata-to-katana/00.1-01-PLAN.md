---
phase: 00.1-rebrand-from-kata-to-katana
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [commands/katana/, katana/]
autonomous: true

must_haves:
  truths:
    - "kata/ directory no longer exists"
    - "katana/ directory exists with all content"
    - "commands/kata/ directory no longer exists"
    - "commands/katana/ directory exists with all command files"
  artifacts:
    - path: "katana/"
      provides: "Main Katana directory with templates, workflows, references"
    - path: "commands/katana/"
      provides: "All Katana slash commands"
  key_links:
    - from: "katana/"
      to: "katana/templates/"
      via: "directory structure preserved"
    - from: "katana/"
      to: "katana/workflows/"
      via: "directory structure preserved"
---

<objective>
Rename top-level directories from Kata to Katana using git mv to preserve history.

Purpose: Directory renames must happen first before any content can be updated, as all file path references depend on the new directory names.
Output: Two directories renamed: kata/ -> katana/, commands/kata/ -> commands/katana/
</objective>

<execution_context>
@~/.claude/katana/workflows/execute-plan.md
@~/.claude/katana/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00.1-rebrand-from-kata-to-katana/00.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename commands/kata to commands/katana</name>
  <files>commands/katana/</files>
  <action>
    Use `git mv commands/kata commands/katana` to rename the commands directory.
    This preserves git history for all 24+ command files inside.
    Verify the directory exists after rename and contains all .md files.
  </action>
  <verify>ls commands/katana/*.md | wc -l (should show 24+ files)</verify>
  <done>commands/katana/ exists with all command files, commands/kata/ no longer exists</done>
</task>

<task type="auto">
  <name>Task 2: Rename kata to katana</name>
  <files>katana/</files>
  <action>
    Use `git mv kata katana` to rename the main project directory.
    This preserves git history for all templates, workflows, and references inside.
    Verify subdirectories (templates/, workflows/, references/) exist after rename.
  </action>
  <verify>ls katana/ (should show templates/, workflows/, references/)</verify>
  <done>katana/ exists with all subdirectories, kata/ no longer exists</done>
</task>

<task type="auto">
  <name>Task 3: Commit directory renames</name>
  <files>commands/katana/, katana/</files>
  <action>
    Stage and commit the directory renames with message:
    "refactor(00.1): rename kata directories to katana"

    Do NOT push yet - this is part of a larger rebrand.
  </action>
  <verify>git log -1 --oneline (shows rename commit)</verify>
  <done>Directory renames committed with descriptive message</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ls kata/` fails (directory gone)
- [ ] `ls katana/` succeeds (directory exists)
- [ ] `ls commands/kata/` fails (directory gone)
- [ ] `ls commands/katana/` succeeds (directory exists)
- [ ] `git status` shows clean (changes committed)
</verification>

<success_criteria>
- Both directory renames completed via git mv
- Git history preserved for all files
- Commit created for directory renames
- No errors or broken symlinks
</success_criteria>

<output>
After completion, create `.planning/phases/00.1-rebrand-from-kata-to-katana/00.1-01-SUMMARY.md`
</output>
